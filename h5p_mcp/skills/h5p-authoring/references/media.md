# media

Reference local assets as asset:<id> in native media fields; map IDs to absolute files in assets. The host controls authorized roots and size budgets. Supply the actual MIME type. Remote URLs are not embedded and require network during playback. Never fetch arbitrary assets on the user's behalf without authorization.

