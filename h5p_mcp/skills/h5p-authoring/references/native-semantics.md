# native-semantics

Use get_h5p_type_contract and read its raw_schema resource for editor metadata. Preserve exact library major/minor versions. Single-field groups contain the child value without an object wrapper. Query nested libraries. Preparation supplies native defaults; do not invent field names. Examples are starting points tied to their library versions.

