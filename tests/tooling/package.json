{
  "name": "h5p-mcp-verification-tooling",
  "private": true,
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@modelcontextprotocol/conformance": "0.1.16",
    "@modelcontextprotocol/server": "2.0.0",
    "@playwright/test": "1.63.0"
  },
  "devDependencies": {
    "@types/bun": "1.4.2",
    "oxlint": "1.83.0",
    "oxlint-tsgolint": "7.0.2002",
    "typescript": "7.0.2"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.domain.json && tsc -p tsconfig.adapters.json",
    "lint": "oxlint --type-aware --deny-warnings domain-probe.ts adapter-probe.ts domain-boundary-probe.ts"
  }
}
